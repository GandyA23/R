# Gandy Esaú Ávila Estrada
# Otros tipos de vectores 

a<-5

# Vectores lógicos
c(T, F, T)
c(T, T, T, a == 20)

# Vectores de tipo caracter
frutas <- c("Manzana", "Guayaba", "Papaya")
frutas

precio <- c(10, 15, 20)

# Se asocian los nombres de las frutas con el precio
names(precio) <- frutas
precio
